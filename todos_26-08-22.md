1. "Parameter" is missing the programmatic generation controls that "Sweep" had. Look through the git history and/or past docs to see what those were (e.g. create a range of N numbers with start A, end B). Basically, "Paramter" is missing the vector-valued component.

2. For UI of a Parameter node in the canvas: get rid of " -> EachOf" at the bottom, get rid of "src" text, and get rid of "N recs" text next to each checkbox's value.

3. "pd.read_csv" is an invalid function, but "pandas.read_csv" is accepted.

4. "2026-08-25 14:08:19.477 INFO  [scistack_gui] [run_thread] Looking up function 'pandas.read_csv' in registry (run_id=rlxjnw3a)
2026-08-25 14:08:19.477 WARN  [scistack_gui] [run_thread] Function not found: "Function 'pandas.read_csv' not found in registry. Did you pass --module or --project with the script that defines it?" (run_id=rlxjnw3a)". "numpy" and "pandas" functions should be executable, and these functions should not be added to the registry, they should instead be found by explicit import.

5. On a function node created by the GUI, the input node is in the bottom left rather than the left middle. It fixed itself after dragging another node to the canvas.