1. "Parameter" is missing the programmatic generation controls that "Sweep" had. Look through the git history and/or past docs to see what those were (e.g. create a range of N numbers with start A, end B). Basically, "Paramter" is missing the vector-valued component.

2. On a function node created by the GUI, the input node is in the bottom left rather than the left middle. It fixed itself after dragging another node to the canvas.

3. ~~The default "Variable File" (as named in the "Paths" popup) should be (a) named "Entities File", and (b) should be relative to the project root, not the datasets folder, (c) should be a TOML file, e.g. "./src/scistack_entities.toml".~~

4. ~~The "fn is defined at: {path}:{line_no}" message should be highlightable/copy-pasteable.~~

5. ~~When trying to run a MATLAB function while using the browser, I am getting the message "Function 'loadDelsysEMGOneFile' not found in registry, and is not an importable library function (numpy/pandas/stdlib). Did you pass --module or --project with the script that defines it?". Is it possible to execute a MATLAB function node while using the browser?~~

6. ~~If a python environment doesn't have scistack_gui, then the user will get a message "Server failed to start". This error message should be improved when running as a VS Code extension~~

7. During the VS Code extension "create new project" wizard, there's a question "How should SciStack discover your pipeline code?" with options "Select a project (pyproject.toml)", "Select a single pipeline module (.py)", and "No module". This step should likely be omitted, as I think that would best match the browser-based flow.

8. ~~When I selected "No module" I got a "SciStack: Server failed to start - Error: Python process exited (code=1, signal=null). Is this the Python process not being one that has the proper packages installed?~~

9. in VS Code Extension, "PATHS" field with the database location does not appear on project creation, only after another path is added. But it's set to the database location on project creation in the browser workflow.

10. "Variable File" is left as "not set - auto-creates on first use" in VS Code, but is auto created in the browser workflow.

11. In VS Code Extension, adding a folder scans the files and adds to the sidebar, but not to the "Discovered Code" section of the "Paths" popup until "Refresh" is clicked. This is different than browser-based workflow.

12. Creating a new variable in the VS Code Extension GUI caused a full re-read of all code files.

13. ~~Clicking "Run" caused "start_run: function=loadDelsysEMGOneFile language=matlab variants=0" "handleMatlabRun: requesting generate_matlab_command for loadDelsysEMGOneFile". It should have run in an attached MATLAB process. The run just hangs instead. It also disconnected the output variable from the function node for some reason.~~

14. ~~PathInputs (and maybe other entities too) that are found on first load may appear on the canvas, but not in the PathInputs list in the sidebar.~~ (fixed 2026-09-01 — see item 20 below; PathInputs was the only list with this gap)

15. Creating a new variable in the GUI sidebar forced a whole codebase refresh.

16. Make default folder for "Select folder for new SciStack database" default to open VS Code workspace.

17. Remove the "No module"/"Single pipeline"/etc. selection step during project creation.

18. The first click on a "Run" button on a MATLAB function node only establishes the MATLAB connection, but is counted as a successful run.

19. Parameters are initialized with a value of 0. This is wrong, they should not be initialized with any value at all.

20. For Parameters with complex values (e.g. dict/struct) I want to be able to edit the scistack_entities.toml to modify the parameter value, and then update/refresh only that parameter. I should also be able to right click on any Parameter node and select to open the scistack_entities.toml file.

---

## 2026-09-01 — `scidb.log` triage

Diagnosed from `/workspace/scidb.log` (17:02:35 → 17:10:33) plus two MATLAB terminal transcripts. Plan and implementation status: `.claude/log-triage-fixes.md`. Concept note: `docs/claude/matlab-output-handle-contract.md`. All implemented, full `scistack-gui` + `scimatlab` suites pass, **uncommitted**.

16. ~~Running the same MATLAB function node a second time failed to parse: "Invalid expression. When calling a function or indexing a variable, use parentheses." A PathInput's `to_key()` JSON blob leaked into `scidb.register_variable(...)`, because `_collect_var_types` (`api/matlab_command.py`) didn't filter PathInput params the way every other consumer does. Only bit on the _second_ run — run 1 has no DB variants and takes the template branch, then records the variant that breaks run 2.~~

17. ~~After a MATLAB function fires, it visually disconnects from its output variable, though the variable still turns green. This is the second half of item 13, which was never fixed when the hang was. Cause: graduation rewrites the manual edge's endpoints to the placement-qualified form (`fn__x__cid::main`), but `infer_manual_fn_param_to_class` compared ids exactly, so `matlab_param_to_class` came back empty; the node then renders handle `out__{param}` while the edge points at `out__{Class}`, and React Flow silently drops an edge whose sourceHandle doesn't exist. Fixed centrally via `edge_resolver.bare_fn_node_ids()`.~~

18. ~~`scidb.Log` / `scifor.Log` truncated Windows paths in every log line. Callers pre-format (`Log.info(sprintf(...))`) and `Log.m` ran `sprintf` again, so `\L` in `y:\LabMembers\...` was eaten as an escape and the message was cut to `y:`. Fixed with a `nargin == 1` guard in all four level methods in both packages.~~

19. ~~Windows DuckDB lock conflicts were surfaced as raw `IOException` tracebacks instead of the graceful `DatabaseLockedError` path. `db.py::_as_locked_error` matched only the POSIX wording; the Windows wording shares no phrase with it, so the 5s retry loop never ran. Three RPCs failed inside a 2.1s window the backoff would have absorbed. Cannot reproduce on Linux CI, so the verbatim message is pinned in the test.~~

20. ~~PathInputs missing from the sidebar on project reopen (item 14). The `EditTab.tsx` mount effect fetched the registry, parameters and notes but not path inputs — those were only fetched on the `dag_updated` broadcast, which is what "Refresh code" sends. All five list fetches in that component were audited; PathInputs was the only omission.~~

21. Excessive refresh frequency / slow startup (items 11, 12, 15). **Improved, not closed.** Three full 141-file rescans in 90s, 12-16s each over the SMB share. `classify_matlab_file` now memoizes on `(mtime_ns, size)`, so an unchanged file costs one `stat` instead of three opens plus three parses (the uncached body reads each file up to 3x: variable parse, function parse, entities-script check). Every `refresh_all()` call site was left as a full refresh **on purpose** — caching cannot change semantics, narrowing what each site refreshes could. Scan duration and cache hit/miss counts are now logged per scan, and `refresh_all` logs its calling frame so repeat scans can be attributed.
    - **Still to do:** confirm from the new log lines whether any refreshes are outright redundant and should be removed rather than just made cheap. Items 12/15 (creating a variable forces a full re-read) may already be fixed by the earlier narrow-register work in `variable_service.py` — worth re-testing before closing.
    - **Trap for future work:** the `(mtime_ns, size)` key cannot see a same-tick, same-size write. Any code that writes a `.m` and later re-scans must call `matlab_parser.invalidate_classify_cache(path)`. Wired into `matlab_registry.reload_source` and `services/variable_service.py`.

22. **Open question from item 17.** The DB `output_num` source of `matlab_param_to_class` contributed nothing when it should have mapped `loaded_data → RawEMG` on its own — meaning the manual-edge "fallback" is actually load-bearing. Either `output_num` is `None` for MATLAB batch saves, or `matlab_output_order[fn]` was empty/too short to index. Now logged at INFO with per-source attribution:

    ```
    [pipeline] matlab_param_to_class: fn=%s from_db_output_num=%s from_manual_edges=%s merged=%s
    ```

    One GUI-driven MATLAB run will settle it. If `from_db_output_num` comes back `{}`, there is a second bug behind the one that was fixed.

23. **Deliberately deferred from item 17.** The defensive fallback in `build_edges` — using the handle the node will actually render, rather than the class name — was not implemented. It requires threading `matlab_output_order` into `build_edges`, which has no concept of MATLAB signature order today. `test_graph_builder.py::TestMatlabOutputHandleInvariant` asserts every edge's `sourceHandle` exists on its source node, which is the real protection; add the parameter when something actually needs it.

24. **Manual verification still outstanding** for items 16, 17, 20, 21 — tests confirm the mechanisms but not the canvas. One session covers all four: run a MATLAB node twice (16 executes, 17 stays wired), reopen the project (20 lists PathInputs without "Refresh code"), and watch for `[matlab_registry] Classified N MATLAB source file(s) in X.XXs (cache: H hit(s), M miss(es) this scan)` — first scan all misses at the old duration, every refresh after nearly all hits (21).
